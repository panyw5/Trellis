# Performance and Caching

Describe the implementation-level performance rules that matter for this project.

Include:

- which computations are expected to be cached
- cache key conventions and invalidation assumptions
- acceptable performance baselines on benchmark cases
- signs that a computation should switch strategy or stop being interactive
- when memory use matters more than runtime, or vice versa
