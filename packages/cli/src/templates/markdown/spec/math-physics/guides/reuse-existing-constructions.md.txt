# Reuse Existing Constructions Guide

Before adding a new construction, search for an equivalent object or helper already present in the project.

Check:

- existing representations of the same mathematical family
- benchmark generators that can be extended instead of duplicated
- cached invariants or lookup tables that already exist
