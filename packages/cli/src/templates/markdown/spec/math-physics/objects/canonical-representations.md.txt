# Canonical Representations

Describe how each core object is encoded in code and how equivalent objects are normalized.

Document:

- canonical ordering rules
- serialization / pretty-print formats
- equality and hashing expectations
- when two presentations should be treated as the same object
